from flask import Flask
from trick import Trick
import sqlite3 as lite


conn = lite.connect(bucketlist.db)

trickid = getTrickId

    if trickid == 0:
        trickid = getTrickId
    else:
        trickid = getTrickId +1
        print trickid




        value1 = nick